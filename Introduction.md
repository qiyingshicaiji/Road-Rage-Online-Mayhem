# 《暴走摩托：双人载具》在线游戏项目说明文档

**文档版本**：1.0  
**最后更新**：2026年3月8日  
**项目状态**：规划阶段  
**目标平台**：Windows（客户端） + Linux（服务器）  

---

## 1. 项目概述

《暴走摩托：双人载具》是一款基于C++/Unity开发的多人在线竞速+战斗游戏。玩家可三人联机，在单一地图上进行混战或组队合作（2人一队）。游戏核心特色是**双人载具机制**：一辆摩托车由两名玩家分别扮演驾驶者和后座乘客，通过配合与干扰对手赢得比赛。项目旨在展示C++高性能服务器开发、网络同步算法、游戏物理模拟以及AI集成能力，为实习申请提供有力作品。

---

## 2. 玩法设计

### 2.1 核心玩法
- **竞速**：玩家驾驶摩托车在固定赛道中竞速，以到达终点的顺序排名。
- **战斗**：玩家可互相干扰，包括踹人、捏对手刹车、使用武器敲打。所有干扰动作均会影响自身车辆稳定性。
- **双人载具**：每辆车可由两名玩家控制（驾驶者+后座），两人需协同操作。
  - **驾驶者**：控制油门、转向、刹车；负责保持车辆平衡，对抗后座动作带来的扰动。
  - **后座乘客**：执行干扰动作，每次动作消耗车辆“平衡值”，导致车辆晃动。

### 2.2 暴力机制（干扰方式）
- **踹人**：后座或驾驶员伸出脚踹邻近对手，造成对方短暂失控（偏移、减速）。
- **捏刹车**：伸手捏对手刹车把手，使其急减速（需靠近且角度合适）。
- **武器敲打**：后座用棒子敲打对手，造成硬直或方向偏移。
- **所有干扰动作均影响自身车辆平衡**：后座动作会产生扰动扭矩，驾驶者需实时调整方向维持稳定。

### 2.3 平衡值系统
- 每辆车有平衡值（0~100），后座每做一次动作消耗10-20点，随时间缓慢恢复（5点/秒）。
- 驾驶者精细操作（如反打方向）可加快恢复。
- 平衡值越低，车辆越易受扰动失控；平衡值归零则车辆打滑、短暂失控。

### 2.4 游戏模式
- **对战模式**：3人各自为战，按到达终点顺序排名。
- **合作模式**：2人一队（共2队，或1队vs AI），两队竞速。队伍成绩以最后一名成员到达时间为准。

---

## 3. 技术架构

### 3.1 整体架构
```
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│  Unity客户端    │ ──→ │   C++游戏服务器  │ ──→ │   数据库/缓存   │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ • 物理表现      │      │ • 网络通信       │      │ • MySQL         │
│ • 动画/UI       │      │ • 房间/匹配      │      │ • Redis         │
│ • 输入采集      │      │ • 游戏逻辑       │      └─────────────────┘
│ • 本地预测      │      │ • 物理权威计算   │
└─────────────────┘      │ • 战斗判定       │
         ↑               │ • AI决策         │
         │               └─────────────────┘
         └─────────── 自定义协议 ───────────┘
```

### 3.2 核心技术栈
| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 游戏服务器 | C++17 + muduo/Boost.Asio | 高性能网络库，Reactor模型 |
| 游戏客户端 | Unity 2022 LTS (C#) | 3D渲染、动画、UI |
| 数据库 | MySQL + Redis | 持久化存储 + 高速缓存 |
| 网络协议 | TCP + UDP | TCP：可靠事件；UDP：实时位置 |
| 序列化 | 自定义二进制协议 | 轻量、可控 |
| 物理引擎 | 服务器：自定义简单物理；客户端：Unity PhysX | 服务器权威计算，客户端表现 |
| AI集成 | 状态机 + 大模型API（可选） | 实现智能对手 |

### 3.3 网络通信协议
- **消息格式**：`[消息ID(2字节)][序列号(2字节)][数据长度(2字节)][数据]`
- **关键消息类型**：
  - `MSG_LOGIN` (C→S)：登录请求
  - `MSG_LOGIN_RESP` (S→C)：登录响应
  - `MSG_CREATE_ROOM` / `MSG_JOIN_ROOM` (C→S)：房间管理
  - `MSG_INPUT` (C→S)：输入帧（驾驶/后座动作）
  - `MSG_SNAPSHOT` (S→C)：全量/增量状态快照
  - `MSG_ATTACK_EVENT` (S→C)：攻击命中/未命中通知
- **粘包处理**：长度前缀法，先发送数据长度，再发送数据。

---

## 4. 核心功能模块

### 4.1 服务器端（C++）
- **网络层**：基于muduo库实现多线程Reactor，支持数千并发连接。
- **游戏逻辑**：
  - **房间管理**：创建/加入/离开房间，分配角色（驾驶/后座）。
  - **物理权威计算**：接收所有玩家输入，按固定时间片（如50ms）更新车辆位置、速度、旋转。使用简化的物理模型（质点+转向力学）保证确定性。
  - **战斗判定**：实现距离/角度检测，延迟补偿（保存200ms历史轨迹）。
  - **平衡值系统**：动态计算平衡值，影响车辆控制。
  - **AI对手**：基于状态机（追逐、攻击、躲避）的简单AI，可选集成大模型API生成对话。
- **数据层**：MySQL连接池，Redis缓存在线状态。

### 4.2 客户端（Unity C#）
- **网络模块**：封装Socket，实现消息收发、序列化/反序列化。
- **输入模块**：采集键盘/手柄输入，打包成`InputFrame`发送。
- **表现层**：
  - 基于Unity物理引擎实现车辆视觉移动（但受服务器快照修正）。
  - 动画系统：攻击动作、受击效果。
  - UI：平衡值条、血量、速度表、聊天框（AI对话）。
- **本地预测**：模拟车辆物理响应输入，收到服务器快照后进行平滑修正。
- **特效与音效**：攻击命中特效、引擎声、碰撞声。

### 4.3 数据库设计
- **MySQL表**：
  - `users`：用户ID、账号、密码、昵称、总场次、胜场。
  - `match_records`：比赛ID、玩家ID、排名、击杀数、用时。
- **Redis缓存**：
  - 在线玩家列表（`online_users:set`）
  - 房间信息（`room:{room_id}:hash`）

---

## 5. 关键技术实现

### 5.1 权威服务器 + 客户端预测
- **服务器**：每50ms进行一次物理更新，生成状态快照广播给所有客户端。
- **客户端**：
  - 发送输入帧（带时间戳）。
  - 立即应用输入更新本地车辆（预测），保证操作手感。
  - 收到服务器快照后，对比本地状态，用插值方式平滑修正位置和旋转。
- **优点**：兼顾公平性与流畅性。

### 5.2 延迟补偿
- 服务器保存过去200ms内所有玩家的位置轨迹（环形缓冲区）。
- 收到攻击请求时，根据请求中的客户端时间戳，回滚到对应时刻进行判定。
- 判定命中后，在当前时刻应用效果（伤害、击退），并通知攻击者及目标。

### 5.3 双人载具输入合并
- 每辆车对应两个玩家ID（驾驶者和后座）。
- 服务器每帧接收两个玩家的输入，合并处理：
  - 驾驶者输入：油门（0~255）、转向（-127~127）、刹车（bool）。
  - 后座输入：动作类型（0无,1踹,2捏刹车,3敲打）、目标ID。
- 物理更新时，先根据驾驶者输入计算基础加速度和转向，再叠加后座动作产生的扰动（随机扭矩），并更新平衡值。

### 5.4 平衡值系统
- 平衡值影响车辆响应灵敏度：平衡值越低，转向增益越小，且更易被扰动。
- 后座动作会立即扣除平衡值并产生扰动扭矩。
- 驾驶者连续稳定驾驶（无剧烈转向）可缓慢恢复平衡值。
- 平衡值归零时触发失控状态：车辆随机偏转并减速，持续1秒。

### 5.5 战斗判定细节
- **踹人**：距离<1.5m，角度差<30°，攻击者与目标相对速度>5m/s（相对靠近）。
- **捏刹车**：距离<1m，目标侧后方，手部位置靠近刹车（通过玩家骨骼模型判定，简化版可用射线检测）。
- **敲打**：距离<2m，武器前向扇形范围（角度60°）。

---

## 6. 开发计划（分阶段里程碑）

| 阶段 | 时间 | 目标 | 产出 |
|------|------|------|------|
| **0-环境搭建** | 1周 | 搭建C++服务器框架，Unity基础网络连通 | Echo服务器，客户端可收发消息 |
| **1-单人驾驶同步** | 2-3周 | 实现单人车辆物理、位置同步 | 单玩家可控制车辆，另一客户端可见 |
| **2-双人载具基础** | 3-4周 | 支持两个客户端控制同一辆车 | 驾驶+后座共存，车辆移动 |
| **3-战斗系统** | 4-5周 | 实现踹人、平衡值系统、攻击反馈 | 可互相攻击，UI显示平衡值 |
| **4-网络优化** | 2-3周 | 延迟补偿、客户端预测、状态插值 | 高延迟下仍流畅，命中判定公平 |
| **5-合作模式+AI** | 3-4周 | 队伍管理、后座扰动、AI对手 | 支持2v2，AI可对话（可选） |
| **6-部署与文档** | 2周 | 部署云服务器，打包客户端，录制演示 | 线上可访问Demo，完整项目文档 |

---

## 7. 项目亮点与创新

- **双人载具合作机制**：独创的驾驶+后座分工，强调团队配合，技术上实现了输入合并与扰动模拟。
- **混合网络同步方案**：UDP实时位置 + TCP可靠事件，延迟补偿保证公平，客户端预测优化手感。
- **软硬结合背景体现**：虽然没有FPGA硬件，但通过底层C++服务器开发展示了扎实的系统编程能力。
- **AI智能体集成**：可选接入大模型，为AI对手赋予人格化对话，紧跟技术潮流。
- **完整工程化实践**：从协议设计、数据库到部署，涵盖游戏服务器开发的完整流程。

---

## 8. 技术选型与依赖

### 8.1 开发环境
- **操作系统**：Windows 11（开发），Ubuntu 22.04（服务器部署）
- **编译器**：MSVC 2022 / GCC 11.4
- **构建工具**：CMake（服务器），Unity Package Manager（客户端）

### 8.2 主要依赖库
- **muduo**：Linux高性能网络库（也可用Boost.Asio跨平台）
- **Protobuf**（可选）：消息序列化（初期可手写）
- **MySQL Connector/C++**：数据库访问
- **hiredis**：Redis客户端
- **Unity**：使用官方Transport Layer API或LiteNetLib（UDP可靠传输）

### 8.3 硬件要求
- **开发机**：8GB+内存，独立显卡（用于Unity渲染）
- **服务器**：1核2GB内存云服务器（学生优惠即可）

---

## 9. 部署与演示

- **服务器部署**：使用Docker打包C++服务器，部署到腾讯云/阿里云轻量应用服务器，开放TCP/UDP端口。
- **客户端分发**：生成Windows可执行文件，上传至网盘或官网供下载。
- **演示视频**：录制多人对战、合作模式、AI对话等精彩片段，上传B站/YouTube。
- **技术博客**：撰写开发心得，分享网络同步、延迟补偿的实现细节，提升简历亮点。

---

## 10. 结语

《暴走摩托：双人载具》项目集C++服务器开发、网络编程、游戏物理、AI应用于一体，充分展示了开发者扎实的计算机基础和工程能力。通过本项目的实施，将为申请游戏公司后端、C++开发、游戏客户端等岗位提供强有力的作品支持。
