# 《暴走摩托：双人载具》项目开发介绍文档

**文档版本**：2.0  
**最后更新**：2026 年 3 月 8 日  
**项目状态**：规划阶段  
**目标平台**：Windows（客户端） · Linux（服务器）  
**仓库地址**：[github.com/qiyingshicaiji/Road-Rage-Online-Mayhem](https://github.com/qiyingshicaiji/Road-Rage-Online-Mayhem)

---

## 目录

1. [项目概述](#1-项目概述)
2. [玩法设计](#2-玩法设计)
3. [技术架构](#3-技术架构)
4. [核心功能模块](#4-核心功能模块)
5. [关键技术实现](#5-关键技术实现)
6. [网络通信协议](#6-网络通信协议)
7. [数据库设计](#7-数据库设计)
8. [可靠性与工程实践](#8-可靠性与工程实践)
9. [开发计划](#9-开发计划)
10. [技术选型与依赖](#10-技术选型与依赖)
11. [项目结构](#11-项目结构)
12. [部署与演示](#12-部署与演示)
13. [项目亮点与创新](#13-项目亮点与创新)
14. [风险与应对策略](#14-风险与应对策略)
15. [结语](#15-结语)

---

## 1. 项目概述

《暴走摩托：双人载具》（Road Rage: Online Mayhem）是一款基于 **C++17 服务器 + Unity 客户端** 开发的多人在线竞速战斗游戏。

玩家可 **2~6 人在线联机**，在赛道上进行混战或组队合作。游戏核心特色是 **双人载具机制**：一辆摩托车由两名玩家分别扮演驾驶者和后座乘客，通过配合操作与干扰对手赢得比赛。

### 项目目标

- 展示 C++ 高性能服务器开发能力（网络编程、并发处理、内存管理）
- 实现完整的网络同步方案（权威服务器、客户端预测、延迟补偿）
- 实践游戏物理模拟与战斗判定系统
- 覆盖从协议设计、数据库、测试到 Docker 部署的全流程工程化实践
- 为游戏公司后端 / C++ 开发 / 游戏客户端等岗位的求职提供有力作品支持

---

## 2. 玩法设计

### 2.1 核心玩法

| 玩法 | 描述 |
|------|------|
| **竞速** | 玩家驾驶摩托车在固定赛道中竞速，以到达终点的顺序排名 |
| **战斗** | 通过踹人、捏刹车、武器敲打等方式干扰对手；所有干扰动作均影响自身车辆稳定性 |
| **双人载具** | 每辆车可由两名玩家控制（驾驶者 + 后座），两人需协同操作 |

**双人载具角色分工：**
- **驾驶者**：控制油门、转向、刹车，负责保持车辆平衡，对抗后座动作带来的扰动
- **后座乘客**：执行干扰动作（踹人 / 捏刹车 / 敲打），每次动作消耗车辆"平衡值"

### 2.2 干扰方式

| 动作 | 触发条件 | 效果 | 对自身影响 |
|------|----------|------|-----------|
| **踹人** | 距离 < 1.5m，角度差 < 30° | 对方短暂失控（偏移、减速） | 消耗平衡值 10 点，产生扰动扭矩 |
| **捏刹车** | 距离 < 1m，目标侧后方 | 使对手急减速 | 消耗平衡值 15 点，产生扰动扭矩 |
| **武器敲打** | 距离 < 2m，前向扇形 60° 范围 | 造成硬直或方向偏移 | 消耗平衡值 20 点，产生较大扰动 |

> 所有干扰动作均产生反向扰动扭矩，驾驶者需实时调整方向维持稳定。这鼓励驾驶者与后座之间的沟通配合。

### 2.3 平衡值系统

```
平衡值范围：0 ~ 100（初始 100）

消耗规则：
  踹人       → -10 点
  捏刹车     → -15 点
  武器敲打   → -20 点

恢复规则：
  自然恢复   → +5 点/秒
  稳定驾驶   → 额外 +3 点/秒（驾驶者无剧烈转向时）

失控触发：
  平衡值 = 0 → 车辆随机偏转 + 减速，持续 1 秒
```

- 平衡值越低，车辆转向增益越小，更易被扰动
- 所有参数支持配置化调整（JSON/配置文件），方便后续平衡性测试
- 平衡值通过状态快照实时同步给客户端，UI 以进度条形式展示

### 2.4 游戏模式

| 模式 | 人数 | 说明 |
|------|------|------|
| **自由混战** | 2~6 人 | 各自为战，按到达终点顺序排名 |
| **组队合作** | 4 人（2v2） | 2 人一队共乘一辆车，以队伍最后成员到达时间为准 |
| **人机对战** | 1~4 人 + AI | 玩家人数不足时由 AI 填补空位 |

> **设计说明**：房间人数采用偶数制以适配双人载具机制。推荐 4 人（2 队 × 2 人）作为标准模式，自由混战可支持 2~6 人。

---

## 3. 技术架构

### 3.1 整体架构

```
┌─────────────────────┐        ┌─────────────────────┐        ┌─────────────────┐
│    Unity 客户端      │  ───→  │   C++ 游戏服务器     │  ───→  │   数据库/缓存    │
├─────────────────────┤        ├─────────────────────┤        ├─────────────────┤
│ • 3D 渲染 / 动画    │        │ • 网络通信 (Asio)    │        │ • MySQL (持久化) │
│ • UI / 输入采集      │        │ • 房间 / 匹配管理    │        │ • Redis (在线态)  │
│ • 客户端预测 + 插值  │        │ • 权威物理计算       │        └─────────────────┘
│ • 音效 / 特效        │        │ • 战斗判定 + 补偿    │
└─────────────────────┘        │ • 平衡值系统        │
          ↑                    │ • AI 状态机         │
          │                    │ • 断线重连 / 心跳    │
          │                    └─────────────────────┘
          └──── TCP (可靠事件) + UDP (实时同步) ──────┘
```

**核心设计原则：**
- **权威服务器**：所有游戏逻辑在服务器端计算，客户端仅负责输入采集和表现渲染
- **客户端预测**：本地立即响应输入，收到服务器快照后平滑修正，兼顾手感与公平
- **混合传输**：TCP 保证房间管理等事件可靠送达，UDP 传输实时位置数据降低延迟

### 3.2 核心技术栈

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 游戏服务器 | C++17 + Boost.Asio | 跨平台高性能异步网络库，Reactor 模型 |
| 游戏客户端 | Unity 2022 LTS (C#) | 3D 渲染、动画系统、物理表现、UI |
| 数据库 | MySQL 8.0 + Redis 6.0 | MySQL 持久化用户/比赛数据；Redis 缓存在线状态和房间信息 |
| 网络协议 | TCP + UDP | TCP：登录 / 房间等可靠事件；UDP：位置 / 状态实时同步 |
| 序列化 | Protobuf (初期) | 降低开发调试成本，待性能瓶颈出现后可局部换用自定义二进制协议 |
| 物理引擎 | 服务器：简化质点物理；客户端：Unity PhysX | 服务器权威计算保证一致性，客户端物理仅用于视觉表现 |
| AI | 有限状态机 (FSM) | 追逐、攻击、躲避基础行为；大模型 API 对话为延伸目标 |
| 单元测试 | Google Test (C++) / Unity Test Framework (C#) | 覆盖核心逻辑的自动化测试 |
| 构建部署 | CMake + Docker | 服务器 Docker 化部署，一键构建 |

---

## 4. 核心功能模块

### 4.1 服务器端（C++）

```
server/src/
├── network/     网络通信层
├── game/        游戏逻辑层
├── combat/      战斗判定模块
├── ai/          AI 行为模块
└── db/          数据访问层
```

| 模块 | 功能 | 关键实现 |
|------|------|----------|
| **网络层** | 基于 Boost.Asio 实现异步 I/O | Reactor 模型，TCP/UDP 双通道；初期目标支持 50~100 人同时在线（约 10~20 个房间） |
| **房间管理** | 创建 / 加入 / 离开房间，分配角色 | 状态机管理房间生命周期（等待 → 游戏中 → 结算）；支持驾驶者 / 后座角色分配 |
| **物理权威计算** | 接收玩家输入，固定时间片更新 | 每 50ms 一次物理更新（20 tick/s）；简化质点 + 转向力学模型 |
| **战斗判定** | 距离 / 角度检测，延迟补偿 | 保存 200ms 历史轨迹（环形缓冲区）；攻击回滚判定 |
| **平衡值系统** | 动态计算平衡值，影响车辆控制 | 参数配置化，支持运行时热更新 |
| **AI 对手** | FSM 实现追逐、攻击、躲避 | 初期使用固定权重状态机；大模型 API 对话为延伸目标 |
| **数据层** | 用户 / 比赛数据持久化 | MySQL 连接池 + Redis 在线状态缓存 |
| **断线重连** | 心跳检测 + session 恢复 | 3 秒心跳间隔，30 秒内可凭 session token 恢复 |

### 4.2 客户端（Unity C#）

```
client/Assets/Scripts/
├── Network/     网络通信模块
├── Vehicle/     车辆控制与物理
├── Combat/      战斗动作与表现
└── UI/          用户界面
```

| 模块 | 功能 | 关键实现 |
|------|------|----------|
| **网络模块** | 消息收发、序列化 / 反序列化 | 封装 TCP/UDP Socket，Protobuf 自动解析 |
| **输入模块** | 采集键盘 / 手柄输入 | 打包成 `InputFrame` 发送至服务器；支持输入缓冲 |
| **本地预测** | 立即响应输入，服务器快照修正 | 预测 → 快照对比 → 插值平滑修正 |
| **车辆表现** | 基于 Unity PhysX 视觉移动 | 受服务器快照驱动，客户端物理仅负责视觉插值 |
| **动画系统** | 攻击动作、受击效果 | 状态机驱动，网络事件触发 |
| **UI 系统** | 平衡值条、速度表、房间列表 | UGUI 实现，数据绑定服务器状态 |
| **音效特效** | 攻击命中、引擎声、碰撞声 | 事件驱动播放 |

---

## 5. 关键技术实现

### 5.1 权威服务器 + 客户端预测

```
                    客户端                              服务器
               ┌──────────────┐                  ┌──────────────┐
    玩家输入 → │ 立即应用本地  │ ── InputFrame → │ 收集所有输入  │
               │ 预测车辆状态  │                  │ 执行物理更新  │
               │              │ ← Snapshot ──── │ 广播状态快照  │
               │ 对比 & 修正  │                  └──────────────┘
               └──────────────┘
```

- **服务器**：每 50ms 进行一次物理更新（20 tick/s），生成状态快照广播
- **增量同步**：首次连接发送全量快照，后续仅发送变化部分（增量压缩），减少带宽占用
- **客户端**：
  - 发送输入帧（带序列号和本地时间戳）
  - 立即应用输入更新本地车辆（预测），保证操作手感
  - 收到服务器快照后，对比本地预测状态，用插值方式平滑修正位置和旋转
- **优点**：兼顾公平性与流畅性

### 5.2 延迟补偿

```
攻击请求 (t=100ms) →  服务器回滚至 t=100ms 的世界状态
                      ↓
                      在历史状态中进行碰撞检测
                      ↓
                   命中 → 在当前时刻应用效果（击退/减速），通知双方
                   未中 → 仅通知攻击者
```

- 服务器保存过去 **200ms** 内所有玩家的位置轨迹（环形缓冲区）
- 收到攻击请求时，根据请求中的客户端时间戳回滚到对应时刻进行判定
- 判定命中后，在当前时刻应用效果（击退、减速），并通知攻击者及目标

### 5.3 双人载具输入合并

每辆车对应两个玩家 ID（驾驶者和后座），服务器每帧合并处理：

```
驾驶者输入:
  ├── 油门:  uint8  (0~255)
  ├── 转向:  int8   (-127 ~ +127)
  └── 刹车:  bool

后座输入:
  ├── 动作类型: enum (无=0, 踹=1, 捏刹车=2, 敲打=3)
  └── 目标ID:   uint32
```

**物理更新流程：**
1. 根据驾驶者输入计算基础加速度和转向角
2. 叠加后座动作产生的扰动扭矩（大小与动作类型相关）
3. 扣除平衡值
4. 应用平衡值系数调整转向增益
5. 更新车辆位置、速度、旋转
6. 输出新的状态快照

### 5.4 战斗判定细节

| 攻击方式 | 距离要求 | 角度要求 | 附加条件 | 效果 |
|----------|---------|---------|---------|------|
| 踹人 | < 1.5m | 角度差 < 30° | 相对速度 > 5m/s | 偏移 + 短暂减速 |
| 捏刹车 | < 1m | 目标侧后方 | 射线检测命中刹车区域 | 急减速 |
| 敲打 | < 2m | 前向扇形 60° | 无 | 硬直 + 方向偏移 |

所有判定均在服务器端执行，使用延迟补偿回滚机制保证公平性。

---

## 6. 网络通信协议

### 6.1 消息格式

```
┌───────────┬──────────┬──────────┬──────────────┬────────────────┐
│ 版本号     │ 消息 ID   │ 序列号    │ 数据长度      │ Protobuf 数据   │
│ (1 byte)  │ (2 bytes)│ (2 bytes)│ (2 bytes)    │ (N bytes)      │
└───────────┴──────────┴──────────┴──────────────┴────────────────┘
```

- **版本号**：保证客户端 / 服务器版本兼容
- **序列号**：用于客户端预测的输入帧对齐和丢包检测
- **粘包处理**：TCP 使用长度前缀法（先发数据长度，再发数据）

### 6.2 关键消息类型

| 消息 | 方向 | 传输层 | 说明 |
|------|------|--------|------|
| `LOGIN` | C → S | TCP | 登录认证请求 |
| `LOGIN_RESP` | S → C | TCP | 登录响应（含 session token） |
| `CREATE_ROOM` | C → S | TCP | 创建房间 |
| `JOIN_ROOM` | C → S | TCP | 加入房间 |
| `LEAVE_ROOM` | C → S | TCP | 离开房间 |
| `ROOM_STATE` | S → C | TCP | 房间状态变更通知 |
| `GAME_START` | S → C | TCP | 游戏开始信号 |
| `INPUT_FRAME` | C → S | UDP | 玩家输入帧（驾驶 / 后座动作） |
| `STATE_SNAPSHOT` | S → C | UDP | 全量 / 增量状态快照 |
| `ATTACK_EVENT` | S → C | TCP | 攻击命中 / 未命中通知 |
| `HEARTBEAT` | C ↔ S | TCP | 心跳保活（每 3 秒） |
| `RECONNECT` | C → S | TCP | 断线重连请求（含 session token） |
| `RECONNECT_RESP` | S → C | TCP | 重连响应（含当前完整状态） |

---

## 7. 数据库设计

### 7.1 MySQL 表结构

**用户表 `users`**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT, PK, AUTO_INCREMENT | 用户唯一 ID |
| `username` | VARCHAR(32), UNIQUE | 账号 |
| `password_hash` | VARCHAR(128) | 密码哈希（bcrypt） |
| `nickname` | VARCHAR(32) | 昵称 |
| `total_matches` | INT, DEFAULT 0 | 总场次 |
| `wins` | INT, DEFAULT 0 | 胜场数 |
| `created_at` | DATETIME | 注册时间 |

**比赛记录表 `match_records`**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT, PK, AUTO_INCREMENT | 记录 ID |
| `match_id` | VARCHAR(36) | 比赛 UUID |
| `user_id` | BIGINT, FK → users.id | 玩家 ID |
| `role` | ENUM('driver','passenger') | 游戏中角色 |
| `rank` | TINYINT | 排名 |
| `attack_count` | INT | 攻击次数 |
| `finish_time_ms` | INT | 完成用时（毫秒） |
| `created_at` | DATETIME | 比赛时间 |

### 7.2 Redis 缓存

| Key | 类型 | 说明 | TTL |
|-----|------|------|-----|
| `online_users` | SET | 在线玩家 ID 集合 | 无（心跳维护） |
| `session:{token}` | HASH | session 信息（user_id, room_id, role） | 5 分钟 |
| `room:{room_id}` | HASH | 房间信息（状态, 玩家列表, 地图） | 房间销毁时删除 |

---

## 8. 可靠性与工程实践

### 8.1 断线重连

```
客户端                                   服务器
  │                                       │
  │──── HEARTBEAT (每 3 秒) ────────→     │
  │                                       │ 超过 9 秒无心跳 → 标记玩家离线
  ×  网络中断                              │ AI 接管该玩家角色
  │                                       │
  │  (30 秒内)                             │
  │──── RECONNECT (session token) ──→     │
  │                                       │ 验证 token → 恢复玩家状态
  │←── RECONNECT_RESP (全量快照) ────     │ 取消 AI 接管
  │                                       │
```

- 客户端每 **3 秒** 发送心跳包
- 服务器 **9 秒** 未收到心跳则标记该玩家离线，AI 接管其角色
- 玩家 **30 秒内** 可凭 session token 重连恢复状态
- 超过 30 秒则 session 失效，需重新登录

### 8.2 错误处理策略

| 场景 | 处理方式 |
|------|----------|
| 消息格式错误 | 记录日志，断开该连接 |
| 协议版本不匹配 | 返回版本不兼容错误码，提示更新客户端 |
| 房间已满 | 返回房间满员错误码 |
| 数据库连接失败 | 自动重试（指数退避），降级为仅缓存模式 |
| 非法输入（速度异常等） | 服务器端校验，丢弃异常输入帧 |

### 8.3 测试策略

| 层级 | 框架 | 覆盖范围 |
|------|------|----------|
| 服务器单元测试 | Google Test | 物理更新、战斗判定、平衡值计算、输入合并 |
| 客户端单元测试 | Unity Test Framework | 网络模块、输入打包、预测修正 |
| 集成测试 | 自定义脚本 | 模拟多客户端连接、断线重连、房间生命周期 |
| 压力测试 | 自定义脚本 | 模拟 50~100 并发连接，验证服务器性能 |

### 8.4 安全考虑

- **密码存储**：bcrypt 哈希，不存储明文
- **输入校验**：服务器端验证所有输入合法性（范围、频率）
- **防作弊**：权威服务器架构天然防止客户端篡改游戏状态
- **连接安全**：TCP 连接使用 session token 认证，token 有过期时间

---

## 9. 开发计划

开发采用 **三阶段渐进式策略**，每阶段结束都有可独立展示的里程碑产出。

### 第一阶段：MVP（最小可行产品）—— 预计 8~10 周

| 阶段 | 时间 | 目标 | 产出 / 验收标准 |
|------|------|------|----------------|
| **P0 - 环境搭建** | 1~2 周 | C++ 服务器框架 + Unity 基础网络连通 | Echo 服务器运行；客户端收发消息验证通过 |
| **P1 - 单人驾驶同步** | 3~4 周 | 单人车辆物理、位置同步、客户端预测 | 两个客户端可见彼此车辆实时移动 |
| **P2 - 房间与匹配** | 2 周 | 房间创建 / 加入 / 离开、角色分配 | 可建房、入房、开始游戏 |
| **P3 - 基础部署** | 1~2 周 | Docker 打包、云服务器部署 | 线上可访问的 MVP Demo |

> **MVP 里程碑**：两个玩家通过互联网连接到服务器，在同一赛道上驾驶摩托车并实时看到对方。

### 第二阶段：核心玩法 —— 预计 8~10 周

| 阶段 | 时间 | 目标 | 产出 / 验收标准 |
|------|------|------|----------------|
| **P4 - 双人载具** | 3~4 周 | 双人控制同一车辆、输入合并、平衡值系统 | 驾驶者 + 后座共存并协作 |
| **P5 - 战斗系统** | 3~4 周 | 踹人 / 捏刹车 / 敲打、命中判定、延迟补偿 | 完整战斗体验，命中判定准确 |
| **P6 - 网络优化** | 2 周 | 增量同步、带宽优化、断线重连 | 200ms 延迟下仍可流畅游玩 |

> **核心玩法里程碑**：4 名玩家分 2 队在线对战，驾驶 + 后座配合攻击对手。

### 第三阶段：完善与扩展 —— 预计 6~8 周

| 阶段 | 时间 | 目标 | 产出 / 验收标准 |
|------|------|------|----------------|
| **P7 - 组队与 AI** | 3~4 周 | 组队模式、FSM AI 对手 | 支持 2v2 和人机对战 |
| **P8 - 美术与音效** | 2 周 | 特效、UI 打磨、音效集成 | 完善的游戏视听体验 |
| **P9 - 文档与演示** | 1~2 周 | 项目文档、演示视频、技术博客 | 完整的求职作品集 |

> **完整产品里程碑**：可公开演示的完整游戏，附带技术文档和演示视频。

### 延伸目标（Stretch Goals）

- 🤖 大模型 API 集成，为 AI 对手赋予人格化对话
- 🏆 排行榜与赛季系统
- 🗺️ 更多赛道地图
- 🎮 手柄操控支持
- 📱 移动端客户端适配

---

## 10. 技术选型与依赖

### 10.1 开发环境

| 项目 | 规格 |
|------|------|
| 开发操作系统 | Windows 10/11（客户端开发），Ubuntu 22.04（服务器开发/部署） |
| C++ 编译器 | GCC 11+ / MSVC 2022（支持 C++17） |
| 构建工具 | CMake 3.20+（服务器）；Unity Package Manager（客户端） |
| 游戏引擎 | Unity 2022 LTS |
| 版本控制 | Git + GitHub |

### 10.2 主要依赖库

| 依赖 | 用途 | 选型理由 |
|------|------|----------|
| **Boost.Asio** | 异步网络 I/O | 跨平台，文档丰富，社区活跃 |
| **Protobuf** | 消息序列化 | 开发调试友好，自动生成代码，后期可按需替换 |
| **MySQL Connector/C++** | 数据库访问 | 官方驱动，稳定可靠 |
| **hiredis** | Redis 客户端 | 轻量高效 C 库 |
| **Google Test** | C++ 单元测试 | 业界标准，易于集成 CMake |
| **spdlog** | 日志库 | 高性能，支持异步日志 |
| **Unity Transport / LiteNetLib** | 客户端 UDP 通信 | 可靠 UDP 传输，Unity 生态兼容 |

### 10.3 硬件要求

| 用途 | 最低配置 | 推荐配置 |
|------|----------|----------|
| 开发机 | 8GB 内存，集成显卡 | 16GB 内存，独立显卡（Unity 渲染） |
| 服务器（开发/测试） | 1 核 2GB 云服务器 | 2 核 4GB（学生优惠即可） |
| 服务器（生产/多房间） | 2 核 4GB | 4 核 8GB+，支持 50~100 人同时在线 |

---

## 11. 项目结构

```
Road-Rage-Online-Mayhem/
├── server/                       # C++ 游戏服务器
│   ├── src/
│   │   ├── network/              # 网络通信层 (Boost.Asio, TCP/UDP)
│   │   ├── game/                 # 游戏逻辑 (房间管理、匹配、物理更新)
│   │   ├── combat/               # 战斗判定、延迟补偿
│   │   ├── ai/                   # AI 有限状态机
│   │   ├── db/                   # 数据库访问层 (MySQL + Redis)
│   │   └── main.cpp              # 服务器入口
│   ├── proto/                    # Protobuf 协议定义 (.proto 文件)
│   ├── config/                   # 服务器配置文件 (JSON)
│   ├── tests/                    # 单元测试 (Google Test)
│   ├── CMakeLists.txt            # CMake 构建脚本
│   └── Dockerfile                # Docker 镜像构建
│
├── client/                       # Unity 客户端
│   ├── Assets/
│   │   ├── Scripts/
│   │   │   ├── Network/          # 网络通信模块
│   │   │   ├── Vehicle/          # 车辆控制、预测、插值
│   │   │   ├── Combat/           # 战斗动作、表现
│   │   │   └── UI/               # 用户界面
│   │   ├── Prefabs/              # 预制体
│   │   ├── Scenes/               # 场景文件
│   │   ├── Audio/                # 音效资源
│   │   └── Tests/                # Unity 测试
│   └── ProjectSettings/
│
├── docs/                         # 项目文档
│   ├── protocol.md               # 协议详细说明
│   ├── deployment.md             # 部署指南
│   └── devlog/                   # 开发日志
│
├── Introduction.md               # 本文件 — 项目开发介绍
└── README.md                     # 项目概览与快速开始
```

---

## 12. 部署与演示

### 12.1 服务器部署

```bash
# 构建 Docker 镜像
cd server
docker build -t road-rage-server .

# 运行服务器
docker run -d \
  --name road-rage \
  -p 9527:9527/tcp \
  -p 9528:9528/udp \
  -e DB_HOST=<mysql_host> \
  -e REDIS_HOST=<redis_host> \
  road-rage-server
```

推荐部署到腾讯云 / 阿里云轻量应用服务器，开放 TCP (9527) 和 UDP (9528) 端口。

### 12.2 客户端分发

- 生成 Windows 可执行文件（Unity Build）
- 上传至网盘或 GitHub Releases 供下载
- 客户端内配置服务器地址即可连接

### 12.3 演示计划

| 演示内容 | 形式 | 平台 |
|----------|------|------|
| 多人对战实况 | 录屏视频 | B 站 / YouTube |
| 技术架构讲解 | 技术博客 | 个人博客 / CSDN / 掘金 |
| 网络同步分析 | 图文教程 | GitHub Wiki / 博客 |
| 项目源码 | 开源仓库 | GitHub |

---

## 13. 项目亮点与创新

| 亮点 | 说明 |
|------|------|
| **双人载具合作机制** | 独创的驾驶 + 后座分工设计，技术上实现了输入合并与扰动模拟 |
| **混合网络同步方案** | TCP 可靠事件 + UDP 实时同步 + 延迟补偿 + 增量压缩，多策略组合优化体验 |
| **权威服务器架构** | C++ 高性能服务器处理所有游戏逻辑，天然防作弊 |
| **完整工程化实践** | 从协议设计、数据库、断线重连、测试到 Docker 部署，覆盖游戏服务器开发全链路 |
| **渐进式开发策略** | MVP 优先，每个阶段都有可展示成果，降低项目失败风险 |
| **配置化平衡调整** | 平衡值参数外置为配置文件，支持热更新和数据驱动的平衡性调优 |

---

## 14. 风险与应对策略

| 风险 | 影响 | 应对策略 |
|------|------|----------|
| C++ 与 Unity 跨语言开发学习曲线陡峭 | 可能延长开发周期 | 分阶段推进，MVP 先跑通最简流程 |
| 双人载具同步复杂度高 | 输入合并和预测修正可能出现抖动 | 先完成单人同步，再增量实现双人逻辑 |
| 网络延迟导致战斗体验差 | 高延迟下命中判定不准 | 延迟补偿 + 合理的容差范围 |
| 个人开发精力有限 | 功能裁剪或质量下降 | 严格执行 MVP 策略，非核心功能列入延伸目标 |
| 服务器性能不足 | 多房间场景卡顿 | 初期控制规模（10~20 房间），架构预留水平扩展能力 |

---

## 15. 结语

《暴走摩托：双人载具》项目集 C++ 服务器开发、网络编程、游戏物理、战斗系统、AI 应用于一体，从底层协议设计到云端部署覆盖完整的游戏后端开发链路。

通过三阶段渐进式开发策略，项目在可控的时间和精力范围内逐步交付可展示的成果。完成后的作品将充分展示开发者在系统编程、网络工程和游戏开发方面的综合能力，为求职提供强有力的技术作品支持。

---

*本文档将随项目进展持续更新。如有问题或建议，欢迎提交 Issue 讨论。*
